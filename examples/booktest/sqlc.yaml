version: "2"
plugins:
  - name: crystal
    process:
      cmd: ../../bin/sqlc-gen-crystal
sql:
  - engine: postgresql
    queries: queries.sql
    schema: schema.sql
    codegen:
      - plugin: crystal
        out: src/db
        options:
          package: "db"
          emit_json_tags: true