version: "2"
plugins:
  - name: crystal
    process:
      cmd: /Users/watzon/Projects/personal/sqlc-gen-crystal/bin/sqlc-gen-crystal
sql:
  - schema: "schema.sql"
    queries: "queries.sql"
    engine: sqlite
    codegen:
      - out: src/db
        plugin: crystal
        options:
          module: "Blog"
          emit_json_tags: true
          emit_db_tags: true